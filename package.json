{
  "name": "@pakastin/palette",
  "version": "0.0.5",
  "description": "Simple color palette",
  "main": "index.js",
  "scripts": {
    "dist": "gh-pages -d dist",
    "dev": "node watch",
    "postversion": "git push --tags",
    "postpublish": "npm run dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "~0.12.0"
  }
}

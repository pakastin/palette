{
  "name": "palette",
  "version": "0.0.1",
  "description": "Simple color palette",
  "main": "index.js",
  "scripts": {
    "dist": "gh-pages -d dist",
    "dev": "node watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "~0.12.0"
  }
}
